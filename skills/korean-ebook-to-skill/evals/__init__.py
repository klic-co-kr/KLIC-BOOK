# evals/ — 판단 골든 + 비교기 + 정성 루브릭 (Task 8)
#
# eval 전용 골든(judgment_cases.json)은 CandidateFile(gen 입력)과
# **스키마가 다름**: {book_slug, chapter_under_test, golden_must_extract[]}.
# candidates.yaml의 title/source_refs/rubric 필드를 쓰지 않는다.
