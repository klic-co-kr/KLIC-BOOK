# Changelog

## 26.08.09 — 2026-08-09

- 단일 Markdown 파일(`BOOK.md`) 입력 지원: H1 단위 장 분할, 첫 제목 블록·`목차`/`차례` 섹션 자동 제외
- 외부 표지 이미지 지원: `editorial.cover_image` 경로 지정 시 풀블리드 이미지 표지, 미지정 시 기존 `cover_theme` 생성 표지로 폴백
- 장 제목 분류 강화: `NN.`·`NN)` 번호 접두사에서 장 번호 추출(표제지 번호·제목 정렬), `Part N.`·`제N부` → PART 분할자, `머리말`·`읽는 법` → 프런트매터(번호 없음), `editorial.part_eyebrow` 추가
- 본문 인라인 시각자료 임베드: 원고의 `<!-- figure-spec -->` 블록을 파싱해 `output` SVG가 `asset_root`에 존재하면 `<figure>`로 본문에 넣고, 없으면 “제작 예정” 자리표시자로 교체(원시 스펙 텍스트 노출 방지). 본문 `<img>` 경로 해석·alt 보정 수정
- `SKILL.md` 입력·표지·장 분류·시각자료 임베드 문서 보강

## 26.08.05 — 2026-08-05

- 스킬명 `korean-editorial-pdf` → `korean-ebook`으로 변경
- KLIC-BOOK 저장소 `skills/` 도메인으로 정식 통합
- Codex(`agents/openai.yaml`)·Claude(`SKILL.md`) 양쪽 호환 유지
- AI tells 회피 가이드(`references/ai-tells.md`) 추가 — PDF 편집 도형·표·타이포에 적용
- `display_name` "한국어 전자책 제작"으로 변경
- 버전 체계를 날짜(YY.MM.DD)로 전환

## 1.2.0 — 2026-08-04

- 목차를 2단 편집에서 **1단 장·절 흐름형 목차**로 전면 변경
- 장 표제지 다음에 삽입할 수 있는 원문 기반 시각 편집 레이어 추가
- `process`, `bridge`, `quadrant`, `ladder`, `principles`, `dashboard`, `network`, `matrix` 레이아웃 지원
- 장별 개념도·흐름도·비교표·판단표를 YAML로 선언하는 `visuals.chapters` 계약 추가
- 시각 페이지 하단에 원문 비대체 고지를 자동 삽입하고 빌드 보고서에 시각 페이지 수 기록
- FDE 시각 편집 프로필을 120쪽·1단 목차 3쪽·시각 요약 12쪽으로 회귀 검증
- 본문 중심 FDE 설정을 `book-config.fde-text-only.yaml`로 분리하고 108쪽 회귀 검증
- 120쪽 전 페이지 렌더링, A4·글꼴·북마크·링크·무결성 검수 통과

## 1.1.0 — 2026-08-04

- FDE 성공본 기준 107쪽·북마크 140개·상위 장 시작 페이지 회귀 프로필 확정
- README의 저장소용 안내·수평선·중복 목차·중복 판권을 설정 기반 편집 변환으로 분리
- 표지 워터마크, 속표지 인용문·판본 문구, 앞부분 머리말, 한국어 날짜 형식 설정 추가
- 장별 CSS 클래스로 특정 장·부록의 페이지 밀도 보정 지원
- FDE 목차 타이포그래피와 페이지 번호 줄바꿈 보정
- PDF 메타데이터 제목과 화면 제목을 별도로 설정하도록 개선
- 빌드 보고서의 전체 북마크 수와 최상위 북마크 수를 정확히 분리

## 1.0.1 — 2026-08-04

- 범용 자동 제목 설정과 FDE 재현 설정 분리
- UTF-8 플래그가 없는 ZIP의 한글 파일명 복구
- `1~30일` 같은 범위 제목의 무결성 검증 오류 수정
- 빈 저자 필드에서 불필요한 `지음` 표시 제거
- 실제 FDE 원고 13개 회귀 테스트와 전 페이지 렌더 검수 통과

## 1.0.0 — 2026-08-04

- 최초 공개
- FDE 한국어판 편집 작업의 출판 PDF 파이프라인을 스킬화
- CONTENT/REFERENCE 강제 분리
- `fde-midnight` 편집 프리셋
- Markdown/ZIP → HTML/WeasyPrint PDF 빌드
- 원문 문단 표본 무결성 검사
- 참고자료 고유 구문 오염 검사
- 글꼴·A4·북마크·링크·빈 페이지 검수
- 전 페이지 렌더링, 접촉표, 완료 보고, ZIP 패키징
